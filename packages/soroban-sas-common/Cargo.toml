[package]
# Publishing requires maintainers to declare the project license first.
publish = false
name = "soroban-sas-common"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
ed25519-dalek = "2.0.0"
soroban-sdk = { workspace = true, features = ["testutils"] }
stellar-strkey = "0.0.8"
